# Upstream changes

Read the fork's recorded upstream baseline, actual remotes and the requested target commit. If no target was selected, choose a justified upstream revision from the current task and show what it changes. Keep the downstream product mainline as its real GitHub canonical default; its name need not be `nimi`.

Fetch the needed upstream history without automatically importing release tags. Review code, dependency, license, CI and instruction-entry changes together. Merge into the working flow required by the repository; do not routinely rebase shared published history, force-push, add a permanent mirror branch or resolve every conflict with one side.

Check affected AI provider initialization, accounts, storage, tooling and agent routing for old paths reintroduced by the merge. Remap new business capabilities through their actual owner. Preserve the original product task, then update the recorded upstream SHA and rerun only affected checks and journeys. App release versions remain independent of upstream tags and component versions.
