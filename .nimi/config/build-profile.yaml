build_profile_ref: electron-packager-pnpm-vite
toolchain_version: node>=24;pnpm>=10;electron=42
build_command: pnpm run build:electron:production
test_command: pnpm run test
output_path: dist-electron-package
shell_output_path: dist-electron
lockfile_path: pnpm-lock.yaml
lockfile_policy: author-install-generates-lockfile
ci_install_command: pnpm install --frozen-lockfile
targets:
  windows-x86_64:
    os: windows
    arch: x86_64
    build_command: pnpm run build:electron:production
    payload_path: dist-electron-package/nimiapp-storybook-shell-win32-x64
    runtime_entry: payload/nimiapp-storybook-shell.exe
profile_role: developer-workflow-input
