# Storybook Spec AGENTS.md

This guidance file points agents to the Storybook authority map. Rule bodies
live in `kernel/*.md` and kernel tables.

## Reading Path

1. Read `.nimi/spec/INDEX.md`.
2. Read `.nimi/spec/storybook/kernel/index.md`.
3. Read the relevant `SBK-*` contract file and table.
4. Use repository `AGENTS.md` for agent workflow and verification guidance.

## Rule ID Format

Storybook kernel rules use `SBK-<DOMAIN>-<NN>`.
